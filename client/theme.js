import { createTheme } from '@mantine/core';

export const title = 'Lumina Solar';
export const year = '2024';

export const theme = createTheme({
  fontFamily: 'Inter, sans-serif',
  primaryColor: 'violet',
});
